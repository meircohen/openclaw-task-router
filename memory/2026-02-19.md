# 2026-02-19

- Implemented native HTML5 drag-and-drop reorder for Queue items in `dashboard.html` (desktop table + mobile cards), added long-press (500ms) arming on touch, visual drag feedback, and POST to `/api/queue/:taskId/priority` with new position priority.
